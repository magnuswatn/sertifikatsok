[project]
name = "testserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "environ-config",
    "ldaptor",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
